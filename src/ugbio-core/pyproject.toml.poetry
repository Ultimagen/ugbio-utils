[tool.poetry]
name = "ugbio-core"
version = "0.1.0"
description = ""
authors = ["avigailmo <avigail.moldovan@ultimagen.com>"]
readme = "README.core.md"
packages = [{ include = "src/ugbio/core" }]


[tool.poetry.dependencies]
python = "^3.12"

[tool.poetry.group.test.dependencies]
pytest = "^8.2.2"
requests-mock = "^1.12.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
